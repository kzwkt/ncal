name: Release Linux Binary

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:

jobs:
  release:
    name: Build & Release Linux
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2

      - name: Inject Release Profile into Cargo.toml
        run: |
          cat << 'EOF' >> Cargo.toml

          [profile.release]
          opt-level = "z"
          lto = true
          codegen-units = 1
          panic = "abort"
          strip = true
          EOF

      - name: Build release binary
        run: cargo build --release --locked

      - name: Strip Debug Symbols
        run: |
          strip target/release/ncal
          ls -lh target/release/ncal

      - name: Package Binary
        run: |
          mkdir -p staging
          cp target/release/ncal staging/
          tar -czvf ncal-linux-x86_64.tar.gz -C staging ncal

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_type == 'tag' && github.ref_name || 'v0.1.0' }}
          files: ncal-linux-x86_64.tar.gz
          generate_release_notes: true
